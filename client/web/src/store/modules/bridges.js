const state = {
  bindings: []
};
const getters = {};
const actions = {};
const mutations = {
  clearBridgesBindingsList(state) {
    state.bindings = [];
  }
};

export default {
  state,
  getters,
  mutations,
  actions
};
